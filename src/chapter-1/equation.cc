// display equation of a line in the form ax + by = c, input a,b,c
#include <iostream>
#include <cmath>

using namespace std;


int main()
{
  int a, b, c;
  cout << "Enter values for a, b and c";
  cin >> a >> b >> c;

  cout << a <<"x + " << b << "y = " << c;
}
